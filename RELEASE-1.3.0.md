# TO-DO Panel 1.3.0 Release 信息

- **版本**：1.3.0（tag：`v1.3.0`）
- **发布日期**：2026-09-15
- **支持平台**：macOS 13.0+ Apple Silicon（arm64）· Windows 10/11 x64

---

## 亮点

- 全新的**「周报」独立页**：本周进度 / 状态 / 下周计划三栏展示，历史周报可查看与删除，AI 一键生成。
- 移除了「首页」Bento 工作台，界面更聚焦：应用默认展开**「待办」**，唤出更快、更干净。
- AI 配置体验补齐：模型从服务端拉取后下拉选择、测试连接只测选中模型、失败如实报错。

## 新增

- **AI 周报独立页**：新增「周报」页签，按「本周进度 / 本周状态 / 下周计划」三栏展示；生成历史可查看与删除，支持一键 AI 生成周报。
- AI 配置支持从服务端拉取模型列表并以下拉选择（如 DeepSeek、MiniMax 等），不再手填模型名。
- 自定义提示词默认显示内置模板，可随时改回内置。

## 变更

- 移除「首页」页签与 Bento 工作台（当前窗口、镜子、快速录音、随笔记、常用指令、汽水音乐、番茄钟卡片）。
- 应用默认展开页改为「待办」；设置的默认展开页被隐藏时，自动回退到第一个可见页。
- 设置页移除「首页组件」与「镜子」配置卡片。

## 修复

- 测试连接只测试当前选中的模型，不再自动切换到其他模型重试；失败如实报错，不做静默替换。
- 周报生成超时从 12 秒放宽到 60 秒，生成期间按钮显示「整理中…」并保持等待，完成后展示结果。
- 修复原生下拉框在深色主题下白底白字看不清的问题。

## 安装包

| 平台 | 安装包 | SHA-256 |
| --- | --- | --- |
| macOS | `TO-DO-Panel-1.3.0-arm64.dmg` | 构建完成后填入 |
| Windows | `TO-DO-Panel-1.3.0-windows-x64-setup.exe` | 构建完成后填入 |

> 官网下载按钮与 README 下载入口均从 GitHub `releases/latest` 动态解析，Release 发布后自动指向本版本安装包。

## 升级说明

- **macOS**：下载 DMG 后拖入「应用程序」替换旧版。ad-hoc 签名分发，首次启动若被拦截，前往「系统设置 → 隐私与安全性」点击「仍要打开」。`safeStorage` 加密的密钥可能需重新填写。
- **Windows**：运行安装向导即可覆盖升级，卸载与升级均保留本机工作区数据。安装包暂无商业签名，可能出现 SmartScreen 提示，请核对来源与 `.sha256` 校验码后继续安装。
- 工作区数据（待办、笔记、链接、录音、设置）本地存储，升级不丢失。
