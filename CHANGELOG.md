# 更新日志

本文件记录 TO-DO Panel 的重要功能、修复与发布变更。尚未发布的改动统一进入「未发布」，创建新版本标签时再归档到对应版本。

## [1.4.0] - 2026-09-17

### 新增
- 待办垃圾篓：普通删除后可恢复，只有永久删除或清空垃圾篓才会删除云端副本。
- 云端删除重试队列：断网或接口失败时保留删除请求，重新连接后自动补发。

### 修复
- 修复待办 ID 重复导致删除或修改一条待办时，其他待办被连带删除或修改的问题；启动时会自动修复历史重复 ID。
- 修复随笔记无法新建、保存后身份不稳定的问题。
- 修复云同步远端变更再次回传、撤销完成不同步，以及设置页出现重复断开操作的问题。
- 修复全量同步尚未完成便显示成功的问题，现在会等待真实结果并显示失败数量。

### 优化
- 同步令牌迁移至 Electron 系统安全存储；旧版本明文令牌会自动迁移并从 LocalStorage 删除。
- 工作区便携数据改用明确白名单，不再导出设备凭据和同步配置。
- 抽离待办垃圾篓、删除队列、周键和同步数据转换等领域逻辑，降低重复代码与模块间隐式依赖。

## [1.3.6] - 2026-09-17

### 新增
- 云端中枢（NexusDesk 同步）存储从 Workers KV 全面迁移到 Cloudflare D1：解决 KV 每日写/列表配额（1,000/日）无法支撑 10 用户量级的问题（旧客户端心跳约 2,880 次/日 KV 写、超配额 2.9 倍）；D1 免费额度为每日 10 万行写入。
- 管理后台「新建成员」表单：输入工号 + 姓名即可建号，重复工号返回 409；新成员默认密码 = 工号，首次登录强制改密。
- 首次登录强制改密弹窗：预填工号作为旧密码、新密码不少于 4 位且两次一致，改密成功后才继续连接云同步。
- 设置页新增「绑定钉钉」按钮（占位，钉钉 ID 后期绑定）。
- WebSocket 实时推送改由 Durable Object 枢纽（WsHub）承载：连接、广播、在线状态集中在单一实例，修复跨 isolate 广播丢失（此前 HTTP 与 WS 落不同实例时推送必然丢）。

### 优化
- 移除 30 秒心跳：云同步只在待办状态变化时增量上报，登录 / 连接时全量对齐一次，无任何定时轮询；断线重连采用指数退避（1s → 30s 封顶）。
- wrangler 升级到 v4；管理后台「在线」标识改查 DO 在线状态，不再依赖单实例内存。

### 清理
- 删除已废弃的 KV 命名空间（USER_DB / TODO_DB），Cloudflare 账号下 KV 列表已清空。

## [1.3.4] - 2026-09-16

### 移除
- 彻底移除录音 / 实时转写功能全套代码（主进程 IPC、转写管道、设置项、录音列表与详情 UI），此前仅隐藏了界面入口、代码一直残留。
- 彻底移除首页「镜子」摄像头镜像功能（IPC、preload 桥、设置预览与相关样式）。
- 移除设置页「显示功能」卡片。

### 修复
- 云端中枢（NexusDesk 同步）无法登录 / 点击无反应：dingtalk-sync.js 存在旧版导出块覆盖新版 API，导致 getSyncConfig 等接口全部失效、设置页初始化直接抛错中断；删除旧覆盖块后恢复。
- 启动崩溃：主进程仍调用已删除的 ensureRecordingsDir；渲染层窗口模块缺失 DOM 引用、设置面板数据获取块被误删，均已修复。
- 设置页「数据文件夹 / 当前版本」一直显示「读取中…」：版本号改为启动后立即推送，不再等待 8 秒后的网络检查；窗口加载期间推送补发，避免状态丢失。
- 检查更新在开发模式卡在「正在检查更新…」：electron-updater 未打包时跳过检查且不触发事件，补兜底状态推送。

### 优化
- 检查更新提速：更新源探测结果缓存 10 分钟（期间免探测直接复用）、探测超时 4s → 2.5s、启动自动检查提前至 3s。

## [1.3.3] - 2026-09-16
## [1.3.3] - 2026-09-16

### 修复
- macOS 创建待办等输入场景看不到输入法候选框：展开态窗口置顶级别从 screen-saver 降为 floating，输入法候选词可正常浮出；折叠态保持 screen-saver，折叠条在菜单栏拦截带内仍可点击。

## [1.3.2] - 2026-09-15

### 新增
- 多更新源自动选择：检查更新时同时探测 GitHub 直连与国内加速镜像（gh-proxy / ghfast），自动选用最快可达的下载源；GitHub 不通时自动切镜像，下载失败也会换源重试。
- 设置页检查更新时显示当前使用的更新源名称。

### 修复
- 检查更新失败时版本号错误显示为 "v"（状态始终注入当前版本号）。
- 检查中（checking）状态误显示「检查更新失败」。

## [1.3.1] - 2026-09-15

### 新增
- 在线升级（Windows）：使用 electron-updater 接入 GitHub Release，检测到新版本后可一键「下载更新」，实时显示下载进度（百分比 / 已下载大小），下载完成点击「重启安装」自动完成升级，无需手动下载安装包。
- 设置页「版本与更新」卡片新增下载进度条与「下载更新 / 重启安装」按钮。

### 变更
- macOS 因 ad-hoc 签名且不公证的正式分发约定，保持「检查更新 + 打开下载页」；Windows 走完整自动升级链路。

### 修复
- Windows 安装冒烟测试适配删除首页后的界面（待办 / 设置 / 凭据 / 通知验证），修复 v1.3.0 首次构建失败。

## [1.3.0] - 2026-09-15

### 新增

- AI 周报独立页：新增「周报」页签，按「本周进度 / 本周状态 / 下周计划」三栏展示，历史周报可查看与删除，支持一键 AI 生成周报。
- AI 配置支持从服务端拉取模型列表并以下拉选择（如 DeepSeek、MiniMax 等），不再手填模型名。
- 自定义提示词默认显示内置模板，可随时改回内置。

### 变更

- 移除「首页」页签与 Bento 工作台（当前窗口、镜子、快速录音、随笔记、常用指令、汽水音乐、番茄钟卡片），应用默认展开「待办」页。
- 默认展开页被隐藏时，自动回退到第一个可见页（原为回退首页）。
- 设置页移除「首页组件」与「镜子」配置卡片。

### 修复

- 测试连接只测试当前选中的模型，不再自动切换到其他模型重试；失败如实报错。
- 周报生成超时从 12 秒放宽到 60 秒，生成期间按钮显示「整理中…」并保持等待。
- 修复原生下拉框在深色主题下白底白字看不清的问题。

## [1.3.5] - 2026-09-16

### 移除
- 彻底移除首页（home-bento 镜子工作台）：常用指令、当前窗口列表、本地汽水音乐、番茄钟、收藏剪贴与全部相关样式 / IPC / 测试，同步清理 AGENTS.md 与 README 描述。

### 新增
- 待办页顶部新增全局待办搜索框：模糊匹配排序（完全相等 > 前缀 > 连续子串 > 按序字符），同时命中待办标题与项目名，点击结果跳转待办页并闪烁定位，最多返回 24 条。

### 修复
- 本周周报错误显示为上一周：weekKey 原按 UTC 截取日期，东八区周一凌晨会被归到周日 key，改用本地年月日拼接后周归属全部对齐。
- 周报页重新进入默认显示本周；仅当选中周存在总结时才沿用历史选择。
- 甘特图字号过小、标题与时长被截断：日期轴与行字号 9.5/11 → 11.5/12.5px，标题列 110 → 170px、时长列 56 → 110px，时长文字不再断行。

### 优化
- 待办历史记录补上项目名称标签（含甘特图悬停提示）。
- 全局搜索框独立放置于待办页顶部工具条，不再挤占 AI 周报胶囊。

## [1.1.2] - 2026-09-12

### 修复

- 修复常驻应用跨天后，新建待办仍沿用昨天或上次默认日期的问题：日期刷新独立于首页时钟，唤醒、展开、切回待办、输入和提交时校准为当天 23:30；保留本条手动选择的日期。

## [1.1.1] - 2026-09-11

### 修复

- 修复实时转写中途断线、报错或会话结束后停止更新的问题：增加自动重连、连接心跳检测和最多 30 秒的待发送音频缓存，保留断线前文字。
- 已有转写文字不再遮住连接异常提示；修复重连期间结束录音的清理与末尾文字保存。

### 官网

- 下载按钮改为上下等宽排列：macOS 在上，Windows 在下；保留原按钮样式与动态下载地址。

## [1.1.0] - 2026-09-07

### 新增

- Windows 10/11 x64 安装包，与 macOS DMG 共用版本及业务代码；新增 Windows 托盘、开机启动、顶部工作区定位及系统加密支持。
- Windows 首页按平台能力隐藏当前窗口和汽水音乐，保留用户组件偏好；剪贴板采用复制后手动 Ctrl+V 粘贴。
- Windows runner 验证安装、真实应用启动、核心 IPC、模拟音视频设备释放、数据保留与卸载；双平台全部通过后统一发布。
- 官网新增「下载 Windows 版本」按钮，与 macOS 下载分别解析最新 Release 对应资产。

### 修复

- 修复恢复工作区备份时自身刷新被导航保护拦截、录音工作区未初始化及便签被旧编辑器覆盖的问题；仍禁止外部导航和打开其他本地文件。

## [1.0.7] - 2026-09-04

### 修复

- 修复首次申请录音或摄像头权限时，macOS 授权弹窗被常驻面板遮挡而无法点击的问题；请求期间临时降低面板层级并激活应用，结束后安全恢复置顶状态。
- 修复摄像头与麦克风权限请求重叠时过早恢复置顶、再次遮挡尚未结束的系统弹窗的问题。
- 修复录音权限等待阶段反复点击不同录音入口会重复发起请求、生成大量错误提示行的问题；等待期间只保留一次启动任务，并显示明确的权限状态。

## [1.0.6] - 2026-09-04

### 新增

- 设置页新增「默认展开页」，可将首页、待办等当前可见 Tab 设为每次唤出面板时的起始页。

### 修复

- 新建待办成功后重置截止时间为当天 23:30，不再沿用上一条待办的手动日期。

## [1.0.5] - 2026-08-31

### 修复

- 修复超长待办提醒被 Node 定时器溢出压缩为 1ms，导致主进程持续循环与 `TimeoutOverflowWarning` 的问题；超过上限的延迟现在分段检查。
- 修复首页组件尺寸切换时的大面积黑块与动画堆积，改为可取消的真实卡片 FLIP 过渡，切换中内容保持可见。
- 减少首页冷启动展开时的重复合成工作，不再同时缩放整个内容层并重播七张卡片的错峰入场。

## [1.0.4] - 2026-08-30

### 修复

- 修复密钥页选中状态无法再次点击取消的问题；取消后批量删除按钮会立即隐藏。
- 修复密钥页批量「删除」按钮错位到搜索框下方的布局问题，选中时保持在搜索框右侧同一行。

## [1.0.3] - 2026-08-30

### 桌面端

- 修复首次启用剪贴板历史时误收录启用前内容，以及文字/图片交替后重复收录同一图片的问题；全新用户目录不再出现预置记录。
- 将剪贴板图片去重改为内容哈希，避免不同图片因尺寸和文件长度相同而被误判。
- 复用 Electron 44 `ClipboardItem` 提供的 PNG 原始字节，并把图片探测降到每 3 秒一次，避免 4K 图片在后台每轮重复编码。
- 修复镜子配图与工作区文件选择器可能被其他应用遮挡的问题，弹窗现在附着主面板且不会触发面板失焦收起。
- 降低展开/收起和首页静置时的 GPU 工作：面板内容层不再参与大面积裁剪，WebGL 动效仅在鼠标悬停时运行。
- 修复多 Tab 分栏时透明容器覆盖整条顶栏、导致展开后无法点击顶部中央空白收起的问题；Tab 点击与 Space 收起保持不变。
- 显式启用渲染沙箱，并拒绝本地窗口导航和创建子窗口。
- 升级至 Electron 44 与 electron-builder 26.15.7，最低系统调整为 macOS 13，并清除完整依赖审计中的已知漏洞。
- 收紧 macOS 运行时 entitlements；启动自检不再在用户尚未同意时触发屏幕捕获 API。
- 通知队列清空后销毁独立通知窗口，避免隐藏 renderer 长期常驻内存。

### 官网

- 新增 Mac 屏幕内的自动 Loading 唤醒动效：等待第二屏截图与首个 TAB 视频就绪，超过 8 秒使用静态封面兜底。
- 将 TAB 演示从 GIF 转换为带 faststart 的无声 H.264 MP4，并使用 WebP 截图作为封面。
- 将第二屏产品截图改为 WebP，并替换为最新的剪贴页面截图。
- 优化 Loading 呼吸幅度、频率、文案尺寸、全屏圆角与壁纸无缝接管效果。

## [1.0.2] - 2026-08-29

### 新增

- 增加常驻面板内的设置页，可配置 API、镜子、功能显示、快捷键、数据目录与开机启动。
- 增加单条密钥删除能力。

### 修复

- 待办截止日期支持跨月选择、自然跨年与正确排序。

## [1.0.1] - 2026-08-28

### 修复

- 在录制页面补齐录音控制，恢复完整的开始、暂停与结束操作。

## [1.0.0] - 2026-08-26

### 发布

- 首个稳定版本，建立固定命名的 Apple Silicon DMG 发布流程。

[1.3.5]: https://github.com/zack3812/todo/compare/v1.3.4...v1.3.5
[未发布]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.1.0...HEAD
[1.1.0]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.7...v1.1.0
[1.0.7]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.6...v1.0.7
[1.0.6]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.5...v1.0.6
[1.0.5]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.4...v1.0.5
[1.0.4]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.3...v1.0.4
[1.0.3]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.2...v1.0.3
[1.0.2]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/xiaopu-ai/TO-DO-Panel/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/xiaopu-ai/TO-DO-Panel/releases/tag/v1.0.0
